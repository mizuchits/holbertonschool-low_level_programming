#include <stdio.h>
#include "main.h"
/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
 */
int print_alphabet_x10(void)
{
    int i;
    for (i = 0; i < 10; i++) {
      char c;
      for (c = 'a'; c <= 'z'; c++) {
	_putchar(c);
      }
      if (i != 9)
	_putchar('\n');
      else
	return (0);
	}	
}
